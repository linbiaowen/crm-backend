apply plugin: "war"

bootWar {
    mainClassName = "com.hthk.crm.CrmwebApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
